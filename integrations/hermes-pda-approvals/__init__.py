"""PDA approval plugin package.

The agent-plugin layer intentionally registers no tools or hooks. The manifest
only participates in Hermes' explicit plugin enable/disable policy; the actual
functionality lives in the authenticated Dashboard extension.
"""
